class Asteroid {

  constructor(counter, x, y, xs, ys) {
    //create new asteroid using Phaser
    //   this = asteroids.create(x, y, 'asteroidBig1')
    //   this.name = 'asteroidBig-' + counter;
    //   this.immovable = false;
    //   game.physics.enable(this, Phaser.Physics.ARCADE);
    //   game.physics.arcade.enable([this])
    //   this.body.velocity.setTo(xs, ys);
    //   this.body.bounce.set(1);
    //   this.body.collideWorldBounds = true
    // //makes asteroid able to collide
    //   this.body.onCollide = new Phaser.Signal();
    //   this.body.onCollide.add(shotDown, this);
  }

}
